{
  "name": "mev-eth-lp",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ mev-eth-lp",
    "create-local": "graph create --node http://localhost:8020/ mev-eth-lp",
    "remove-local": "graph remove --node http://localhost:8020/ mev-eth-lp",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mev-eth-lp"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.67.2",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
